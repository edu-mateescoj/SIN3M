Matériel : raspberry + câble usb-a micro-usb (alimentation) + carte SD.

Un ou deux writerdeck complet dans la salle (celui de Julien G. par exemple) avec son écran. Ou juste 2 ou 3 adaptateurs pour faire des tests avec les écrans de l’école. 

Objectif : configurer un OS et aménager un environnement logiciel d’écriture.

Durée : une journée complète - à découper.

1) Sur PC : Flashage de la carte SD modifiée pour simplifier l'installation: contrôle intégrité du fichier (hash)
2) Sur PC (internet) : installation Obsidian + ipconfig
3) Sur Rpi : Installation de l’OS --> config auto? -raspiconfig (disposition de clavier Qwertz, wifi)
4) PC sur eth_GLiNet & Rpi sur WLAN_GLiNet -->Accès SSH
	1) observations réeaux PC: ipconfig && Rpi: ss (?)
	2) contrôle à distance du Rpi : création dossier, écriture fichier html (?)
	3) Rpi serveur (python) de fichiers : création d'un fichier sur Rpi affiché dans le navigateur du PC
	
5) Installation du logiciel d’écriture :  wordgrinder, focuswriter, etc. (choix possible avec un comparatif ?) → les élèves devront justifier leurs choix techniques
6) Solution de partage : syncthing (vérifier si ça tourne sur un RP0 2w) ou un partage réseau
7) Boot en mode kiosque (dernière tentative, je n’y arrivais pas pour ma part)
8) Personnalisation de l’environnement d’écriture (couleur, typographie, marges, etc. à peaufiner sur les logiciels tournant sur un terminal)

Rendu : un setup complet pour écrire, en justifiant les choix d’ergonomie.

Évaluation : la carte sd est glissée dans le rasperry connecté à un écran, vérifier que tout fonctionne (clavier reconnu + boot en mode kiosque + interface d’écriture et partage de fichier).
Le Rpi sert les fichiers distants : vault obsidian. Objectif évaluable : les modifs distantes sont visibles
